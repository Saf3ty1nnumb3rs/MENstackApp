<h1>Whisky Producing Countries</h1>
<ul>{{#each countries}}
    <div class="card container">

        <a href="/country/{{_id}}">
            <img class="round-image" src="{{img}}" alt="{{country}}">
            <li>{{country}}</li>
        </a>

    </div>
    {{/each}}
</ul>